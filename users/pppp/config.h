#pragma once

/* QMK behavior configuration */
#define TAPPING_TERM    160 // Tap-hold decision time (ms)
#define COMBO_TERM      30  // Time window to press combos (ms)
#define PERMISSIVE_HOLD     // Allow hold if other key is pressed during tap
